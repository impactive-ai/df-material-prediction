name: Predict material price - commodity

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      dt: string
      input: string
      ext: string
      output: string
    command: "python main.py -d {dt} -i {input} -x {ext} -o {output}"
  
